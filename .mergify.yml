pull_request_rules:
  - name: a7 branch variant
    conditions:
      - base = master
    actions:
      comment:
        message: "a7 branch config variant - should NEVER run (head config must be ignored)"
