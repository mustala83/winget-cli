pull_request_rules:
  - name: a7 nested render probe
    conditions:
      - base = master
      - title ~= ^A7NEST
    actions:
      post_check:
        title: "PC {{ 1/0 }}"
        summary: "PCS ok"
      comment:
        message: "NEST-START {{ body }} NEST-END"
# a7
